- name: list collections
  ansible.builtin.shell:
    cmd: "ls /home/runner/collections/ansible_collections/{{ item }}"
  register: _collections
  with_items:
    - community
    - amazon
    - cloud

- name: Display collections
  ansible.builtin.debug:
    var: _collections